import numpy as np

"""
When adding new parameters, add the default values to this config file to add them to
all old config files automatically. Make sure this parameter does not change
the algorithm of the old files.
"""

momentum = 0.9